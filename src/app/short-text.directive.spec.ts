import { ShortTextDirective } from './short-text.directive';

describe('ShortTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortTextDirective();
    expect(directive).toBeTruthy();
  });
});
